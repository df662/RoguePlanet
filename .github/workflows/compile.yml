name: Compile RoguePlanet

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1

    - name: Compile
      run: cl /DUNICODE /D_UNICODE RoguePlanet.cpp /link kernel32.lib bcrypt.lib taskschd.lib comsupp.lib virtdisk.lib ntdll.lib Rpcrt4.lib shlwapi.lib ole32.lib

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: RoguePlanet.exe
        path: RoguePlanet.exe
